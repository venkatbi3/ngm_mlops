name: CD - UAT

on:
  workflow_dispatch:
    inputs:
      git_sha:
        description: 'Git SHA to deploy (defaults to HEAD)'
        required: false
  push:
    branches: [ uat ]

env:
  DATABRICKS_HOST: https://adb-1108654228307752.12.azuredatabricks.net

jobs:
  deploy-uat:
    name: Deploy to UAT
    runs-on: ubuntu-latest

    env:
      DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install databricks-cli>=0.200.0  # Install NEW CLI first

      - name: Install project dependencies
        run: |        
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Validate imports
        run: |
          python -c "from src.common.config import load_model_config; print('✓ Config module loaded')"
          python -c "from src.common.logger import setup_logging; print('✓ Logger module loaded')"
          python -c "from src.common.exceptions import PipelineError; print('✓ Exceptions module loaded')"

      - name: Run integration tests
        run: pytest -v tests/integration/ || true

      - name: Validate Databricks bundle
        run: |
          echo "Validating bundle configuration..."
          databricks bundle validate -t uat

      - name: Deploy to Databricks
        run: |
          echo "Deploying to Databricks Uat environment..."
          databricks bundle deploy -t uat --var="git_sha=${{ github.sha }}"

      - name: Deployment complete
        run: |
          echo "✅ Deployment to Uat completed successfully"
          echo "📦 Git SHA: ${{ github.sha }}"
          echo "🔗 Workspace: ~/Bundles/ngm-mlops-uat"