name: CD - Prod

on:
  workflow_dispatch:
    inputs:
      git_sha:
        description: 'Git SHA to deploy (defaults to HEAD)'
        required: false
  push:
    branches: [ prod ]

env:
  DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST_PROD || 'https://adb-xxxx.xx.azuredatabricks.net' }}

jobs:
  deploy-prod:
    name: Deploy to Prod
    runs-on: ubuntu-latest

    environment:
      name: production
      url: https://databricks.com

    env:
      DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip 
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi

      - name: Install Databricks CLI
        uses: databricks/setup-cli@main    

      - name: Verify Databricks CLI
        run: |
          echo "=== Databricks CLI Version ==="
          databricks --version

      - name: Validate imports
        run: |
          python -c "from src.common.config import load_model_config; print('✓ Config module loaded')"
          python -c "from src.common.logger import setup_logging; print('✓ Logger module loaded')"
          python -c "from src.common.exceptions import PipelineError; print('✓ Exceptions module loaded')"

      - name: Run smoke tests
        run: pytest -v tests/unit/test_smoke.py --tb=short

      - name: Validate Databricks bundle
        run: |
          echo "Validating bundle configuration for Prod..."
          databricks bundle validate -t prod --var="databricks_host=${{ env.DATABRICKS_HOST }}"

      - name: Deploy to Databricks
        run: |
          echo "Deploying to Databricks Prod environment..."
          GIT_SHA=${{ github.event.inputs.git_sha || github.sha }}
          databricks bundle deploy -t prod \
            --var="databricks_host=${{ env.DATABRICKS_HOST }}" \
            --var="git_sha=${GIT_SHA}"

      - name: Deployment complete
        run: |
          echo "✅ Deployment to Prod completed successfully"
          echo "📦 Git SHA: ${{ github.event.inputs.git_sha || github.sha }}"
          echo "🔗 Target: Prod (prod catalog)"